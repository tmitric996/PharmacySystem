import { ScheduleExaminationRequest } from './schedule-examination-request';

describe('ScheduleExaminationRequest', () => {
  it('should create an instance', () => {
    expect(new ScheduleExaminationRequest()).toBeTruthy();
  });
});
