import { CanCancelReservation } from './can-cancel-reservation';

describe('CanCancelReservation', () => {
  it('should create an instance', () => {
    expect(new CanCancelReservation()).toBeTruthy();
  });
});
