import { MedicineReservation } from './medicine-reservation';

describe('MedicineReservation', () => {
  it('should create an instance', () => {
    expect(new MedicineReservation()).toBeTruthy();
  });
});
