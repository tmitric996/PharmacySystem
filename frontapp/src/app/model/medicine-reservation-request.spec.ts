import { MedicineReservationRequest } from './medicine-reservation-request';

describe('MedicineReservationRequest', () => {
  it('should create an instance', () => {
    expect(new MedicineReservationRequest()).toBeTruthy();
  });
});
