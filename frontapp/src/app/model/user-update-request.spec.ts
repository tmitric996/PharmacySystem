import { UserUpdateRequest } from './user-update-request';

describe('UserUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new UserUpdateRequest()).toBeTruthy();
  });
});
